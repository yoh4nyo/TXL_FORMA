spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/502}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update

stripe.api.key=${STRIPE_API_KEY:}

# Keep-alive : empêche Render free tier de mettre le serveur en veille
# Sur Render, ajouter la variable d'env : APP_KEEP_ALIVE_URL=https://txl-forma.onrender.com/api/health
app.keep-alive.url=${APP_KEEP_ALIVE_URL:}